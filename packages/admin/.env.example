# Local dev only.
#
# Vite exposes VITE_* variables to the browser bundle. Do NOT put real secrets
# here unless the admin UI is strictly internal (e.g. localhost only / behind Access).

# Must match the worker's ADMIN_TOKEN (set in the repo root .dev.vars).
VITE_ADMIN_TOKEN=devtoken
